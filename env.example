# Allgemeine Einstellungen
# ------------------------
# Primäre Zeitzone für Container und geplante Jobs. Stimmt idealerweise
# mit der Serverregion überein, damit Cron-/Scheduler-Zeiten korrekt laufen.
TZ=Europe/Berlin

# Backend (FastAPI)
# -----------------
# Timeout in Sekunden für IMAP-Operationen. Erhöhe den Wert für langsame
# Mailserver oder Netze mit hoher Latenz. Werte <= 0 werden ignoriert.
IMAP_CLIENT_TIMEOUT=180

# Frontend (Vite)
# ---------------
# Öffentliche Basis-URL für die API-Aufrufe des Web-Frontends. Verwende eine
# absolute URL im Produktionsbetrieb (z.B. hinter einem Reverse Proxy) oder
# lasse den Wert auf /api, wenn Frontend und Backend unter derselben Domain
# mit API-Pfad ausgeliefert werden.
VITE_API_BASE=https://calsync.example.com/api
